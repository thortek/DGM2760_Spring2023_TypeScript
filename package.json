{
  "name": "mytypescript",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watchTailWind": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "buildTailWind": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "serveVite": "npx vite --port 3000",
    "preview": "npx vite preview",
    "dev": "npm-run-all --parallel --race watchTailWind serveVite",
    "build": "npx tsc && npm run buildTailWind && npx vite build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.4",
    "typescript": "^4.9.4",
    "vite": "^4.0.4"
  },
  "dependencies": {
    "daisyui": "^2.49.0"
  }
}
