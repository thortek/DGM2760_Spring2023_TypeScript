{
  "name": "mytypescript",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compileTS": "npx tsc -w",
    "compileTailWind": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify --watch",
    "server": "lite-server",
    "dev": "npm-run-all --parallel --race compileTS compileTailWind server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.4",
    "typescript": "^4.9.4"
  }
}
